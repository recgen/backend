name: set up uv

runs:
  using: 'composite'
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "0.9.17"
        enable-cache: true

    - name: sync dependencies
      shell: bash
      run: uv sync --locked --all-extras --dev
